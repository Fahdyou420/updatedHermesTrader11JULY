# Windows Host Dependencies
# These packages run on the Windows host (NOT inside Docker containers).
# Install with: pip install -r requirements-host.txt
#
# Run by: hermes_mcp_server.py (port 7779), hermes_rpc/server.py (port 7778)

# MetaTrader5 native Python API — Windows-only, provides direct C++ bridge to MT5 terminal
MetaTrader5

# Web framework for the MCP server and RPC server
fastapi>=0.100.0
uvicorn>=0.22.0

# HTTP client
requests>=2.31.0
httpx>=0.24.0

# Environment
python-dotenv>=1.0.0

# Data
pydantic>=2.0.0
yfinance>=0.2.0
